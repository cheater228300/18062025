/*
Условие задачи 021:
21. Напишите программу, где есть глобальная переменная x, локальная переменная x в функции и блочная переменная x внутри {}. Выведите все три значения и объясните, как работает область видимости.
*/

#include <stdio.h>

// Глобальная переменная
int x = 10;

void func() {
    // Локальная переменная, которая "скрывает" глобальную в этой функции
    int x = 20;

    printf("Локальная x в func(): %d\n", x);

    {
        // Блочная переменная, скрывающая локальную x внутри этого блока
        int x = 30;
        printf("Блочная x внутри {}: %d\n", x);
    }

    // После блока снова доступна локальная x
    printf("Локальная x после блока: %d\n", x);
}

int main() {
    printf("Глобальная x в main(): %d\n", x);
    func();
    printf("Глобальная x после вызова func(): %d\n", x);

    return 0;
}
